# Keep reference-grid settings out of scenario state

Reference-grid mode, mode-specific intervals, and appearance persist as one device-wide display preference rather than scenario data, while visibility lasts only for the open map session. A shared scenario therefore cannot impose an operator's grid, yet a device retains its preferred MGRS or latitude/longitude setup across scenarios; every newly opened map starts with the overlay hidden.
